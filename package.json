{
  "name": "iw4madmin-match-stats-api",
  "version": "2.0.12",
  "private": true,
  "description": "IW4MAdmin plugin that syncs leaderboard snapshots to the 360 Arena API",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/watch.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
